main: net.chamosmp.irene.IrenePlugin
loader: net.chamosmp.irene.IreneLoader

name: Irene

authors: [ "SQD Studios" ]
description: ${description}
version: ${version}

api-version: 1.21

dependencies:
  server:
    LuckPerms:
      load: BEFORE
      required: true
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true